server:
  port: 8080
  servlet:
    context-path: /
spring:
  thymeleaf:
    mode: HTML
  profiles:
    active: dev
  messages:
    basename: i18n/messages
logging:
  config: classpath:logback-spring.xml
